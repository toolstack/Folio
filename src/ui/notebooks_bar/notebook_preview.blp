using Gtk 4.0;

template PaperNotebookPreview : Box {

  halign: center;

  styles ["notebook"]

  Label label {
    hexpand: false;
    vexpand: false;
    halign: start;
    valign: end;
  }

  Image icon {
    hexpand: true;
    vexpand: false;
    halign: center;
    valign: center;
  }
}