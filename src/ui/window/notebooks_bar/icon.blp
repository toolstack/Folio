using Gtk 4.0;

template PaperNotebookIcon : Box {

  hexpand: true;

  accessibility {
    labelled-by: icon;
  }

  Overlay {
    hexpand: true;

    [overlay]
    Box {
      halign: start;
      styles ["marker"]
    }

    [overlay]
    .PaperNotebookPreview icon {}
  }
}