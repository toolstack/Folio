using Gtk 4.0;

template PaperNotebookIcon : Box {

  hexpand: true;

  Overlay {
    hexpand: true;

    [overlay]
    Box {
      halign: start;
      styles ["marker"]
    }

    [overlay]
    .PaperNotebookPreview icon {}
  }
}